{
    "buildCommand": "cd frontend && cd backend && npm install && npm run dev",
    "outputDirectory": "frontend/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/server.js"
        }
    ]
}